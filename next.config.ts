import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Начиная с Next.js 15+, этот параметр лежит на верхнем уровне!
  allowedDevOrigins: ['riverbank-salutary-paralegal.ngrok-free.dev'],
};

export default nextConfig;